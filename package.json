{
  "name": "autitus-bot",
  "version": "1.0.0",
  "private": true,
  "description": "polls autius calendar for available classes and notifies via discord",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "discover": "tsx src/discover.ts",
    "check": "tsx src/check.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "playwright": "^1.51.0",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  }
}
